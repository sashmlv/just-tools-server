/**
 * Server protocol
 * @readonly
 * @enum {string}
 */
const ServerProtocol = Object.freeze({

   http:   'http',
   https:  'https',
   http2:  'http2',
   http2s: 'http2s',
});

export default ServerProtocol;
